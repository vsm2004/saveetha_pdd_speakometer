<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="20dp">

    <View
        android:id="@+id/view_premium_bg"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/circle_premium_bg"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_premium_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/premium_png"
        android:contentDescription="@null"
        app:layout_constraintBottom_toBottomOf="@id/view_premium_bg"
        app:layout_constraintEnd_toEndOf="@id/view_premium_bg"
        app:layout_constraintStart_toStartOf="@id/view_premium_bg"
        app:layout_constraintTop_toTopOf="@id/view_premium_bg" />

    <TextView
        android:id="@+id/tv_premium_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:text="Unlock Premium"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@id/view_premium_bg"
        app:layout_constraintTop_toTopOf="@id/view_premium_bg" />

    <TextView
        android:id="@+id/tv_premium_desc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Get detailed analytics, practice\nexercises, and progress reports"
        android:textColor="#B3FFFFFF"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@id/tv_premium_title"
        app:layout_constraintTop_toBottomOf="@id/tv_premium_title" />

    <TextView
        android:id="@+id/tv_learn_more"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Learn more →"
        android:textColor="#FF4081"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="@id/tv_premium_title"
        app:layout_constraintTop_toBottomOf="@id/tv_premium_desc" />

</androidx.constraintlayout.widget.ConstraintLayout>